def run_stage01_extract(context):
    """
    Stage 01

    File validation
    PDF extraction
    HTML extraction
    Page extraction
    Semantic reconstruction
    OCR reconstruction
    Header/body generation

    Returns updated context.
    """
    return context
