module.exports = {
  apps: [

    // ============================
    // 🚀 Backend API
    // ============================
    {
      name: "sl-backend",
      script: "dist/server.js",
      cwd: "/var/www/solvelitigation/backend",
      node_args: "--max-old-space-size=512",
      max_memory_restart: "2G",
      autorestart: true,
      watch: false,
      env: {
        NODE_ENV: "production",
        PORT: 4000,
        AUTH_COOKIE_NAME: "sl_auth",
        NLP_BASE_URL: "http://127.0.0.1:8000",
        JWT_SECRET: "90664e3508dbf4862cee37fa12f446a816a91332bf76a5a590a0c729",
        SYSTEM_INGEST_USER_ID: "69841d4f065476f7a38de668",

        MONGO_URI: "mongodb://sl_admin:solvelitigation%401966@127.0.0.1:27017/solvelitigation?authSource=admin",

        INGESTION_TIMEOUT_MINUTES: 30,
        INGESTION_MAX_RETRIES: 3,
        INGESTION_MAX_CONCURRENT: 8,
        INGESTION_MAX_QUEUE: 200
      }
    },

    // ============================
    // 🔄 Ingestion Worker
    // ============================
    {
      name: "sl-ingestion-worker",
      script: "dist/workers/ingestionProcessing.worker.js",
      cwd: "/var/www/solvelitigation/backend",
      node_args: "--max-old-space-size=256",
      max_memory_restart: "500M",
      autorestart: true,
      watch: false,
      env: {
        NODE_ENV: "production",
        MONGO_URI: "mongodb://sl_admin:solvelitigation%401966@127.0.0.1:27017/solvelitigation?authSource=admin",
        NLP_BASE_URL: "http://127.0.0.1:8000",
        INGESTION_MAX_CONCURRENT: 8,
        INGESTION_MAX_QUEUE: 200,
        INGESTION_AUTO_ENQUEUE_INTERVAL: 15000
      }
    },

    // ============================
    // 🧠 NLP API (FINAL STABLE FIX)
    // ============================
    {
      name: "sl-nlp-api",
      cwd: "/var/www/solvelitigation/nlp_service",

      script: "/var/www/solvelitigation/nlp_service/.venv/bin/python",
      args: "-m uvicorn app.main:app --host 0.0.0.0 --port 8000",

      interpreter: "none",
      exec_mode: "fork",

      autorestart: true,
      watch: false,

      max_memory_restart: "2G",

      env: {
        PYTHONPATH: "/var/www/solvelitigation/nlp_service"
      }
    },

    // ============================
    // ⚙️ Celery Worker
    // ============================
    {
      name: "sl-celery-worker",
      cwd: "/var/www/solvelitigation/nlp_service",

      script: "/var/www/solvelitigation/nlp_service/.venv/bin/python",
      args: "-m celery -A app.celery_app worker -Q celery -l info",

      interpreter: "none",
      exec_mode: "fork",

      autorestart: true,
      watch: false,

      max_memory_restart: "2G",

      env: {
        REDIS_URL: "redis://:StrongRedisPassword2026!@127.0.0.1:6379/0",
        PYTHONPATH: "/var/www/solvelitigation/nlp_service"
      }
    },

    // ============================
    // 🌐 Frontend (Next.js)
    // ============================
    {
      name: "sl-frontend",
      script: "node",
      args: ".next/standalone/server.js",
      cwd: "/var/www/solvelitigation/frontend",

      node_args: "--max-old-space-size=512",
      max_memory_restart: "2G",

      autorestart: true,
      watch: false,

      env: {
        NODE_ENV: "production",
        PORT: 3000
      }
    }

  ]
};
